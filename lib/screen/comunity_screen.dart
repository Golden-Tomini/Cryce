import 'package:flutter/material.dart';

class ComunityScreen extends StatelessWidget{
  const ComunityScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context){
    return const Scaffold(
      body: Center(
        child: Text('comscreen'),
      ),
    );
  }
}